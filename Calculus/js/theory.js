// ── Теория в стиле Степика ───────────────────────────────
import { showPage } from './ui.js'

const THEORY_DATA = {
  integrals: {
    title: 'Интегралы',
    accent: '#3b82f6',
    icon: '∫',
    backTo: 'integralsSection',
    steps: [
      {
        title: 'Степенная функция',
        formula: '\\[\\int x^n\\,dx = \\frac{x^{n+1}}{n+1} + C, \\quad n \\neq -1\\]',
        note: 'Увеличиваем показатель на 1 и делим на новый показатель. Пример: \\(\\int x^3\\,dx = \\frac{x^4}{4}+C\\)',
        detail: `<p>Правило простое: берёшь степень \\(n\\), прибавляешь к ней 1 — это станет новой степенью, и на это же число делишь. Это обратная операция к производной: там степень <em>уменьшалась</em>, здесь — <em>увеличивается</em>.</p>
<p>Работает для любых степеней: целых, дробных, отрицательных. Единственное исключение — \\(n=-1\\), там получается деление на ноль (для этого есть следующий шаг про логарифм).</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\int x^4\\,dx = \\dfrac{x^5}{5}+C\\) — была степень 4, стала 5, делим на 5.</p>
<p>2)&nbsp; \\(\\int x^{-3}\\,dx = \\dfrac{x^{-2}}{-2}+C = -\\dfrac{1}{2x^2}+C\\) — отрицательная степень: \\(-3+1=-2\\), делим на \\(-2\\).</p>
<p>3)&nbsp; \\(\\int\\!\\sqrt{x}\\,dx = \\int x^{1/2}\\,dx = \\dfrac{x^{3/2}}{3/2}+C = \\dfrac{2}{3}x^{3/2}+C\\) — корень это степень \\(\\tfrac{1}{2}\\), прибавляем 1, получаем \\(\\tfrac{3}{2}\\).</p>`
      },
      {
        title: 'Логарифмический интеграл',
        formula: '\\[\\int \\frac{1}{x}\\,dx = \\ln|x| + C\\]',
        note: 'Частный случай при n = −1. Модуль нужен, т.к. ln определён только для x > 0. Пример: \\(\\int \\frac{3}{x}\\,dx = 3\\ln|x|+C\\)',
        detail: `<p>Если подставить \\(n=-1\\) в формулу степенного интеграла, в знаменателе появится ноль — это недопустимо. Поэтому \\(\\int\\frac{1}{x}\\,dx\\) — отдельный случай, и его результат — натуральный логарифм.</p>
<p>Модуль нужен, потому что логарифм определён только для положительных чисел, а \\(x\\) может быть и отрицательным. Формула с модулем работает при любом \\(x\\neq 0\\).</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\int\\dfrac{1}{x}\\,dx = \\ln|x|+C\\) — базовый случай.</p>
<p>2)&nbsp; \\(\\int\\dfrac{5}{x}\\,dx = 5\\ln|x|+C\\) — константа выносится за знак интеграла.</p>
<p>3)&nbsp; \\(\\int\\dfrac{1}{3x}\\,dx = \\dfrac{1}{3}\\ln|x|+C\\) — \\(\\tfrac{1}{3}\\) это просто множитель, он выносится.</p>`
      },
      {
        title: 'Экспонента',
        formula: '\\[\\int e^x\\,dx = e^x + C \\qquad \\int a^x\\,dx = \\frac{a^x}{\\ln a} + C\\]',
        note: 'Экспонента — единственная функция, равная своей производной и своему интегралу. Пример: \\(\\int e^{2x}\\,dx = \\frac{1}{2}e^{2x}+C\\)',
        detail: `<p>Функция \\(e^x\\) уникальна: она равна своей производной — и, как следствие, своему интегралу. Это её главное свойство. При интегрировании она остаётся собой, плюс константа.</p>
<p>Для любого другого основания \\(a\\) появляется деление на \\(\\ln a\\) — это легко запомнить: при дифференцировании \\(a^x\\) умножается на \\(\\ln a\\), при интегрировании — делится.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\int e^x\\,dx = e^x+C\\) — ничего не меняется.</p>
<p>2)&nbsp; \\(\\int 3e^x\\,dx = 3e^x+C\\) — константа просто выносится.</p>
<p>3)&nbsp; \\(\\int 2^x\\,dx = \\dfrac{2^x}{\\ln 2}+C\\) — основание \\(a=2\\), в знаменателе \\(\\ln 2 \\approx 0{,}693\\).</p>`
      },
      {
        title: 'Тригонометрия',
        formula: '\\[\\int \\sin x\\,dx = -\\cos x + C \\qquad \\int \\cos x\\,dx = \\sin x + C\\]',
        note: 'При интегрировании синуса появляется минус. Пример: \\(\\int \\sin 3x\\,dx = -\\frac{1}{3}\\cos 3x+C\\)',
        detail: `<p>Главная ловушка здесь — минус у синуса. Его очень часто забывают! Запомни: интеграл от косинуса — синус (без минуса, всё хорошо), интеграл от синуса — минус косинус (минус обязательно!).</p>
<p>Проверить себя легко: возьми производную от \\(-\\cos x\\) — получишь \\(\\sin x\\). Значит, \\(\\int\\sin x\\,dx = -\\cos x+C\\). Всё сходится.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\int\\cos x\\,dx = \\sin x+C\\) — без минуса.</p>
<p>2)&nbsp; \\(\\int\\sin x\\,dx = -\\cos x+C\\) — обязательно с минусом!</p>
<p>3)&nbsp; \\(\\int 4\\sin x\\,dx = -4\\cos x+C\\) — константа остаётся, минус никуда не девается.</p>`
      },
      {
        title: 'Линейная замена',
        formula: '\\[\\int f(ax + b)\\,dx = \\frac{1}{a}\\,F(ax + b) + C\\]',
        note: 'Линейный аргумент (ax + b) → делим результат на a. Пример: \\(\\int (2x+1)^4\\,dx = \\frac{(2x+1)^5}{10}+C\\)',
        detail: `<p>Когда внутри функции стоит не просто \\(x\\), а \\(ax+b\\), интегрируем как обычно (по таблице), но в конце <strong>делим на коэффициент \\(a\\)</strong> при \\(x\\). Это ускоренный вариант метода подстановки для линейных аргументов.</p>
<p>Смысл: замена \\(t=ax+b\\), \\(dt=a\\,dx\\), то есть \\(dx=\\frac{dt}{a}\\) — вот откуда делитель \\(a\\).</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\int(2x+1)^4\\,dx = \\dfrac{(2x+1)^5}{5\\cdot 2}+C = \\dfrac{(2x+1)^5}{10}+C\\) — делим на \\(a=2\\).</p>
<p>2)&nbsp; \\(\\int\\sin(3x)\\,dx = -\\dfrac{1}{3}\\cos(3x)+C\\) — интегрировали синус (поставили минус), делим на \\(a=3\\).</p>
<p>3)&nbsp; \\(\\int e^{5x-2}\\,dx = \\dfrac{1}{5}e^{5x-2}+C\\) — экспонента остаётся сама собой, делим на \\(a=5\\).</p>`
      },
      {
        title: 'Метод подстановки',
        formula: '\\[\\int f(g(x))\\cdot g\'(x)\\,dx = F(g(x)) + C\\]',
        note: 'Замена t = g(x), dt = g′(x)dx. Пример: \\(\\int 2x\\cdot e^{x^2}\\,dx = e^{x^2}+C\\) (t = x²)',
        detail: `<p>Смотришь на интеграл и ищешь "внутреннюю" функцию \\(g(x)\\), производная которой <strong>тоже стоит под знаком интеграла</strong>. Замена \\(t=g(x)\\), тогда \\(dt=g'(x)\\,dx\\), и всё упрощается до табличного интеграла от \\(t\\).</p>
<p>После нахождения интеграла по \\(t\\) — не забудь вернуться обратно и заменить \\(t\\) на \\(g(x)\\).</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\int 2x\\cdot e^{x^2}\\,dx\\): видим \\(e^{x^2}\\) и рядом \\(2x\\) — это производная \\(x^2\\). Замена \\(t=x^2\\), \\(dt=2x\\,dx\\) → \\(\\int e^t\\,dt = e^{x^2}+C\\).</p>
<p>2)&nbsp; \\(\\int\\dfrac{\\cos x}{\\sin^2 x}\\,dx\\): замена \\(t=\\sin x\\), \\(dt=\\cos x\\,dx\\) → \\(\\int t^{-2}\\,dt = -\\dfrac{1}{\\sin x}+C\\).</p>
<p>3)&nbsp; \\(\\int\\dfrac{x}{x^2+1}\\,dx\\): замена \\(t=x^2+1\\), \\(dt=2x\\,dx\\) → \\(\\dfrac{1}{2}\\int\\dfrac{dt}{t} = \\dfrac{\\ln(x^2+1)}{2}+C\\).</p>`
      },
      {
        title: 'Интегрирование по частям',
        formula: '\\[\\int u\\,dv = uv - \\int v\\,du\\]',
        note: 'Правило ЛАТЕ: Логарифм, Алгебра, Тригонометрия, Экспонента — выбираем u в этом порядке. Пример: \\(\\int x e^x\\,dx = xe^x - e^x + C\\)',
        detail: `<p>Используется, когда под интегралом — произведение функций разных типов (полином × экспонента, полином × тригонометрия и т.д.). Выбираешь \\(u\\) и \\(dv\\): \\(u\\) будет дифференцироваться, \\(dv\\) — интегрироваться.</p>
<p>По правилу <strong>ЛАТЕ</strong> выбираем \\(u\\) в порядке: Логарифм → Алгебра → Тригонометрия → Экспонента. Логарифм неудобно интегрировать, поэтому его берут за \\(u\\) в первую очередь.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\int xe^x\\,dx\\): \\(u=x\\), \\(dv=e^x\\,dx\\) → \\(du=dx\\), \\(v=e^x\\) → \\(xe^x-\\int e^x\\,dx = xe^x-e^x+C\\).</p>
<p>2)&nbsp; \\(\\int x\\cos x\\,dx\\): \\(u=x\\), \\(dv=\\cos x\\,dx\\) → \\(x\\sin x-\\int\\sin x\\,dx = x\\sin x+\\cos x+C\\).</p>
<p>3)&nbsp; \\(\\int\\ln x\\,dx\\): \\(u=\\ln x\\), \\(dv=dx\\) → \\(x\\ln x-\\int x\\cdot\\frac{1}{x}\\,dx = x\\ln x-x+C\\).</p>`
      },
      {
        title: 'Свойства неопределённого интеграла',
        formula: '\\[\\int (\\alpha f + \\beta g)\\,dx = \\alpha\\int f\\,dx + \\beta\\int g\\,dx\\]',
        note: 'Интеграл линеен: константу выносим за знак, интеграл суммы — сумма интегралов. Пример: \\(\\int (3x^2 + 5)\\,dx = x^3 + 5x + C\\)',
        detail: `<p>Интегрирование — линейная операция. Это означает две вещи: во-первых, константу всегда можно вынести за знак интеграла; во-вторых, интеграл суммы равен сумме интегралов. Это делает работу с полиномами очень удобной.</p>
<p>Благодаря этому свойству любой полином интегрируется "по кускам" — каждое слагаемое отдельно.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\int(3x^2+5)\\,dx = 3\\cdot\\dfrac{x^3}{3}+5x+C = x^3+5x+C\\).</p>
<p>2)&nbsp; \\(\\int(2e^x-3\\cos x)\\,dx = 2e^x-3\\sin x+C\\) — каждый член интегрируем отдельно.</p>
<p>3)&nbsp; \\(\\int\\!\\left(x^3-\\dfrac{2}{x}+\\sqrt{x}\\right)\\!dx = \\dfrac{x^4}{4}-2\\ln|x|+\\dfrac{2}{3}x^{3/2}+C\\).</p>`
      }
    ]
  },

  derivatives: {
    title: 'Производные',
    accent: '#eab308',
    icon: "f'(x)",
    backTo: 'derivativesSection',
    steps: [
      {
        title: 'Степенная функция',
        formula: "\\[(x^n)'= nx^{n-1}\\]",
        note: `Степень выносим вперёд, уменьшаем на 1. Примеры: \\((x^5)'=5x^4\\), \\((\\sqrt{x})'=\\frac{1}{2\\sqrt{x}}\\)`,
        detail: `<p>Берёшь степень, умножаешь на неё, и уменьшаешь степень на 1. Это, пожалуй, самая часто используемая формула в дифференцировании. Работает для любых степеней — целых, дробных, отрицательных.</p>
<p>Это обратно тому, что делает интеграл: там степень увеличивается, здесь — уменьшается.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\((x^5)' = 5x^4\\) — степень 5 вышла вперёд, показатель стал \\(5-1=4\\).</p>
<p>2)&nbsp; \\((x^3)' = 3x^2\\) — аналогично.</p>
<p>3)&nbsp; \\((\\sqrt{x})' = (x^{1/2})' = \\dfrac{1}{2}x^{-1/2} = \\dfrac{1}{2\\sqrt{x}}\\) — корень это степень \\(\\tfrac{1}{2}\\), применяем то же правило.</p>`
      },
      {
        title: 'Экспонента и логарифм',
        formula: "\\[(e^x)'= e^x \\qquad (\\ln x)'= \\frac{1}{x} \\qquad (a^x)'= a^x\\ln a\\]",
        note: `Производная eˣ — сам eˣ. Пример: \\((e^{3x})'=3e^{3x}\\), \\((\\ln 5x)'=\\frac{1}{x}\\)`,
        detail: `<p>Функция \\(e^x\\) уникальна тем, что её производная равна ей самой. Это её ключевое свойство. Для любого другого основания \\(a\\) при дифференцировании появляется множитель \\(\\ln a\\).</p>
<p>Производная логарифма \\(\\ln x\\) — это \\(\\frac{1}{x}\\). Это тоже нужно знать наизусть — встречается очень часто.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\((e^x)' = e^x\\) — никаких изменений.</p>
<p>2)&nbsp; \\((\\ln x)' = \\dfrac{1}{x}\\) — запомнить обязательно.</p>
<p>3)&nbsp; \\((e^{3x})' = 3e^{3x}\\) — цепное правило: производная \\(e^{...}\\) это \\(e^{...}\\), умножаем на производную аргумента \\(3x\\), то есть на 3.</p>`
      },
      {
        title: 'Тригонометрия',
        formula: "\\[(\\sin x)'= \\cos x \\qquad (\\cos x)'= -\\sin x\\]",
        note: 'Производная синуса — косинус (без минуса). Производная косинуса — минус синус. Дальше по кругу.',
        detail: `<p>Эти две формулы нужно знать наизусть. Запомни: \\(\\sin \\to \\cos\\) (без минуса, всё хорошо), \\(\\cos \\to -\\sin\\) (минус появляется!). При дальнейшем дифференцировании идёт цикл: \\(\\sin\\to\\cos\\to{-\\sin}\\to{-\\cos}\\to\\sin\\).</p>
<p>Ещё способ: производная синуса — это синус, "сдвинутый" на \\(\\frac{\\pi}{2}\\) влево, то есть косинус.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\((\\sin x)' = \\cos x\\) — без минуса.</p>
<p>2)&nbsp; \\((\\cos x)' = -\\sin x\\) — минус обязателен!</p>
<p>3)&nbsp; \\((3\\sin x)' = 3\\cos x\\) — константа выносится.</p>`
      },
      {
        title: 'Тригонометрия — продолжение',
        formula: "\\[(\\tan x)'= \\frac{1}{\\cos^2 x} \\qquad (\\cot x)'= -\\frac{1}{\\sin^2 x}\\]",
        note: `Пример: \\((\\tan 2x)'=\\frac{2}{\\cos^2 2x}\\). Не забываем цепное правило!`,
        detail: `<p>Тангенс это \\(\\frac{\\sin x}{\\cos x}\\), поэтому его производную можно вывести через правило частного. Но проще просто запомнить результат: \\(\\frac{1}{\\cos^2 x}\\). Котангенс — аналогично, только со знаком минус и синусом в знаменателе.</p>
<p>При применении к сложным аргументам не забывай умножать на производную аргумента (цепное правило).</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\((\\tan x)' = \\dfrac{1}{\\cos^2 x}\\) — основная формула.</p>
<p>2)&nbsp; \\((\\tan 2x)' = \\dfrac{2}{\\cos^2 2x}\\) — цепное правило: умножили на производную \\(2x\\), то есть на 2.</p>
<p>3)&nbsp; \\((\\cot x)' = -\\dfrac{1}{\\sin^2 x}\\) — аналогично тангенсу, но минус и синус.</p>`
      },
      {
        title: 'Правило произведения',
        formula: "\\[(uv)'= u'v + uv'\\]",
        note: `Каждый множитель дифференцируется по очереди, результаты складываются. Пример: \\((x^2 e^x)'=2xe^x+x^2 e^x\\)`,
        detail: `<p>Когда нужно дифференцировать произведение двух функций, нельзя просто перемножить их производные — это ошибка! Правило: дифференцируешь первый множитель (второй оставляешь), потом дифференцируешь второй (первый оставляешь), и складываешь.</p>
<p>Аналогия: два участника, каждый "работает" по очереди, пока другой "стоит".</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\((x^2\\cdot e^x)' = 2x\\cdot e^x + x^2\\cdot e^x = xe^x(2+x)\\).</p>
<p>2)&nbsp; \\((x\\cdot\\sin x)' = 1\\cdot\\sin x + x\\cdot\\cos x = \\sin x + x\\cos x\\).</p>
<p>3)&nbsp; \\((x^3\\cdot\\ln x)' = 3x^2\\cdot\\ln x + x^3\\cdot\\dfrac{1}{x} = 3x^2\\ln x + x^2\\).</p>`
      },
      {
        title: 'Правило частного',
        formula: "\\[\\left(\\frac{u}{v}\\right)'= \\frac{u'v - uv'}{v^2}\\]",
        note: `"Числитель минус числитель, делённые на знаменатель в квадрате". Пример: \\(\\left(\\frac{x}{e^x}\\right)'=\\frac{e^x-xe^x}{e^{2x}}\\)`,
        detail: `<p>Производная дроби — это не дробь производных! Используем формулу: производная числителя умножается на знаменатель, <strong>минус</strong> числитель умножается на производную знаменателя, всё делится на знаменатель в квадрате.</p>
<p>Способ запомнить: <em>"числитель-штрих × знаменатель − числитель × знаменатель-штрих, всё на знаменатель²"</em>. Знак минус в середине — важен!</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\left(\\dfrac{x}{e^x}\\right)' = \\dfrac{1\\cdot e^x - x\\cdot e^x}{e^{2x}} = \\dfrac{e^x(1-x)}{e^{2x}} = \\dfrac{1-x}{e^x}\\).</p>
<p>2)&nbsp; \\(\\left(\\dfrac{\\sin x}{x}\\right)' = \\dfrac{\\cos x\\cdot x - \\sin x\\cdot 1}{x^2} = \\dfrac{x\\cos x - \\sin x}{x^2}\\).</p>
<p>3)&nbsp; \\(\\left(\\dfrac{x^2}{x+1}\\right)' = \\dfrac{2x(x+1)-x^2}{(x+1)^2} = \\dfrac{x^2+2x}{(x+1)^2}\\).</p>`
      },
      {
        title: 'Цепное правило',
        formula: "\\[(f(g(x)))'= f'(g(x))\\cdot g'(x)\\]",
        note: `Внешняя производная умножается на внутреннюю. Пример: \\((\\sin x^2)'=\\cos(x^2)\\cdot 2x\\)`,
        detail: `<p>Когда одна функция "вложена" в другую, применяем цепное правило: сначала берём производную <strong>внешней</strong> функции (внутреннее выражение пока не трогаем), затем умножаем на производную <strong>внутренней</strong>.</p>
<p>Думай об этом как о слоях: снимаешь внешний слой (берёшь его производную), потом умножаешь на производную того, что внутри.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\((\\sin(x^2))' = \\cos(x^2)\\cdot 2x\\) — внешняя: \\(\\sin\\to\\cos\\); внутренняя: \\(x^2\\to 2x\\).</p>
<p>2)&nbsp; \\((e^{3x^2})' = e^{3x^2}\\cdot 6x\\) — внешняя: \\(e^{...}\\to e^{...}\\); внутренняя: \\(3x^2\\to 6x\\).</p>
<p>3)&nbsp; \\(((x^2+1)^5)' = 5(x^2+1)^4\\cdot 2x = 10x(x^2+1)^4\\).</p>`
      },
      {
        title: 'Обратные тригонометрические',
        formula: "\\[(\\arcsin x)'= \\frac{1}{\\sqrt{1-x^2}} \\qquad (\\arctan x)'= \\frac{1}{1+x^2}\\]",
        note: 'Используются при интегрировании выражений с \\(\\sqrt{1-x^2}\\) и \\(1+x^2\\).',
        detail: `<p>Эти формулы особенно важны при интегрировании: если видишь в знаменателе \\(\\sqrt{1-x^2}\\) или \\(1+x^2\\) — скорее всего, ответ будет через арксинус или арктангенс.</p>
<p>Оба результата — рациональные выражения, хотя исходные функции — тригонометрические. Их стоит знать наизусть.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\((\\arcsin x)' = \\dfrac{1}{\\sqrt{1-x^2}}\\) — корень и квадрат под ним.</p>
<p>2)&nbsp; \\((\\arctan x)' = \\dfrac{1}{1+x^2}\\) — сумма 1 и квадрата.</p>
<p>3)&nbsp; \\((\\arctan(2x))' = \\dfrac{2}{1+(2x)^2} = \\dfrac{2}{1+4x^2}\\) — цепное правило: умножаем на производную \\(2x\\), то есть на 2.</p>`
      }
    ]
  },

  series: {
    title: 'Ряды и последовательности',
    accent: '#ef4444',
    icon: '∑',
    backTo: 'seriesSection',
    steps: [
      {
        title: 'Сходимость ряда',
        formula: '\\[S = \\sum_{n=1}^{\\infty} a_n = \\lim_{N\\to\\infty} S_N, \\quad S_N = a_1 + a_2 + \\cdots + a_N\\]',
        note: 'Ряд сходится, если последовательность частичных сумм имеет конечный предел.',
        detail: `<p>Ряд — это бесконечная сумма. Вопрос: может ли бесконечная сумма иметь конечный результат? Да — если слагаемые убывают достаточно быстро.</p>
<p>Идея: составляем частичные суммы \\(S_1, S_2, S_3,\\ldots\\) и смотрим, к чему они стремятся. Если к конечному числу — ряд <strong>сходится</strong>. Если уходят в бесконечность — <strong>расходится</strong>.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\sum_{n=1}^{\\infty}\\dfrac{1}{2^n} = \\dfrac{1}{2}+\\dfrac{1}{4}+\\dfrac{1}{8}+\\cdots = 1\\) — геометрический ряд с \\(q=\\tfrac{1}{2}<1\\), сходится.</p>
<p>2)&nbsp; \\(\\sum_{n=1}^{\\infty}\\dfrac{1}{n^2} = 1+\\dfrac{1}{4}+\\dfrac{1}{9}+\\cdots = \\dfrac{\\pi^2}{6}\\) — сходится.</p>
<p>3)&nbsp; \\(\\sum_{n=1}^{\\infty} 1 = 1+1+1+\\cdots\\) — расходится, частичные суммы растут без предела.</p>`
      },
      {
        title: 'Необходимый признак расходимости',
        formula: '\\[\\lim_{n\\to\\infty} a_n \\neq 0 \\implies \\sum a_n \\text{ расходится}\\]',
        note: 'Если общий член не стремится к нулю — ряд точно расходится. Обратное неверно! Пример: гармонический ряд \\(\\sum \\frac{1}{n}\\) расходится, хотя \\(\\frac{1}{n}\\to 0\\).',
        detail: `<p>Это первая проверка при работе с рядами: смотришь, стремится ли общий член \\(a_n\\) к нулю. Если нет — ряд точно расходится, и дальше можно не думать.</p>
<p>Но осторожно: если \\(a_n\\to 0\\) — это ещё <strong>не значит</strong>, что ряд сходится. Это только необходимое условие. Классический пример: гармонический ряд \\(\\sum\\frac{1}{n}\\) — там \\(\\frac{1}{n}\\to 0\\), но ряд всё равно расходится!</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\sum\\dfrac{n}{n+1}\\): \\(\\lim\\dfrac{n}{n+1}=1\\neq 0\\) → ряд расходится сразу.</p>
<p>2)&nbsp; \\(\\sum(-1)^n\\): предел не существует \\(\\neq 0\\) → расходится.</p>
<p>3)&nbsp; \\(\\sum\\dfrac{1}{n}\\): \\(\\lim\\dfrac{1}{n}=0\\), но ряд расходится — нужны дополнительные признаки.</p>`
      },
      {
        title: 'Признак Даламбера',
        formula: '\\[L = \\lim_{n\\to\\infty}\\left|\\frac{a_{n+1}}{a_n}\\right|: \\quad L < 1 \\Rightarrow \\text{сх.}, \\quad L > 1 \\Rightarrow \\text{расх.}, \\quad L=1 \\Rightarrow \\text{?}\\]',
        note: 'Удобен при наличии факториалов и показательных функций. Пример: \\(\\sum\\frac{n!}{n^n}\\) — Даламбер даёт L < 1, сходится.',
        detail: `<p>Берёшь отношение соседних членов и находишь предел. Если предел меньше 1 — каждый следующий член значительно меньше предыдущего, ряд сходится. Больше 1 — члены растут, ряд расходится.</p>
<p>Признак особенно хорошо работает, когда есть факториалы (\\(n!\\)) или степени (\\(a^n\\)) — они красиво сокращаются. Если \\(L=1\\) — признак не даёт ответа, нужен другой метод.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\sum\\dfrac{2^n}{n!}\\): \\(\\dfrac{a_{n+1}}{a_n}=\\dfrac{2}{n+1}\\to 0 < 1\\) → сходится.</p>
<p>2)&nbsp; \\(\\sum\\dfrac{n!}{2^n}\\): \\(\\dfrac{a_{n+1}}{a_n}=\\dfrac{n+1}{2}\\to\\infty > 1\\) → расходится.</p>
<p>3)&nbsp; \\(\\sum\\dfrac{n^n}{n!}\\): \\(\\dfrac{a_{n+1}}{a_n}=\\left(1+\\dfrac{1}{n}\\right)^n\\to e > 1\\) → расходится.</p>`
      },
      {
        title: 'Признак Коши (радикальный)',
        formula: '\\[L = \\lim_{n\\to\\infty}\\sqrt[n]{|a_n|}: \\quad L < 1 \\Rightarrow \\text{сходится}, \\quad L > 1 \\Rightarrow \\text{расходится}\\]',
        note: 'Удобен, когда aₙ имеет вид (f(n))ⁿ. Пример: \\(\\sum\\left(\\frac{2n}{n+1}\\right)^n\\) — Коши даёт L = 2 > 1, расходится.',
        detail: `<p>Берём корень степени \\(n\\) из \\(|a_n|\\) и находим предел. Этот признак особенно удобен, когда общий член уже является чем-то в степени \\(n\\) — тогда корень красиво упрощает выражение.</p>
<p>Правило то же, что у Даламбера: меньше 1 — сходится, больше 1 — расходится, равно 1 — неопределённость.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\sum\\left(\\dfrac{2n}{n+1}\\right)^n\\): \\(\\sqrt[n]{a_n}=\\dfrac{2n}{n+1}\\to 2 > 1\\) → расходится.</p>
<p>2)&nbsp; \\(\\sum\\left(\\dfrac{1}{3}\\right)^n\\): \\(\\sqrt[n]{a_n}=\\dfrac{1}{3} < 1\\) → сходится (геометрический).</p>
<p>3)&nbsp; \\(\\sum\\dfrac{1}{n^n}\\): \\(\\sqrt[n]{a_n}=\\dfrac{1}{n}\\to 0 < 1\\) → сходится.</p>`
      },
      {
        title: 'Признак Лейбница',
        formula: '\\[\\text{Если } |a_n| \\searrow 0, \\text{ то } \\sum_{n=1}^{\\infty}(-1)^{n-1}a_n \\text{ сходится}\\]',
        note: 'Для знакочередующихся рядов. Пример: \\(\\sum\\frac{(-1)^{n-1}}{n} = 1 - \\frac{1}{2} + \\frac{1}{3} - \\cdots = \\ln 2\\)',
        detail: `<p>Этот признак работает только для <strong>знакочередующихся</strong> рядов — там, где знаки чередуются через один. Условие простое: модули членов монотонно убывают и стремятся к нулю.</p>
<p>Интуиция: каждое следующее слагаемое частично "компенсирует" предыдущее — и поскольку они убывают к нулю, суммирование стабилизируется.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\sum\\dfrac{(-1)^{n-1}}{n} = 1-\\dfrac{1}{2}+\\dfrac{1}{3}-\\cdots\\): \\(a_n=\\dfrac{1}{n}\\searrow 0\\) → сходится, сумма \\(= \\ln 2\\).</p>
<p>2)&nbsp; \\(\\sum\\dfrac{(-1)^n}{n^2}\\): \\(a_n=\\dfrac{1}{n^2}\\searrow 0\\) → сходится.</p>
<p>3)&nbsp; \\(\\sum(-1)^n\\): \\(a_n=1\\) не стремится к нулю → признак Лейбница неприменим, ряд расходится.</p>`
      },
      {
        title: 'Признак сравнения',
        formula: '\\[0 \\le a_n \\le b_n:\\quad \\sum b_n \\text{ сх.} \\Rightarrow \\sum a_n \\text{ сх.}; \\quad \\sum a_n \\text{ расх.} \\Rightarrow \\sum b_n \\text{ расх.}\\]',
        note: 'Сравниваем с эталонным рядом (геометрическим или обобщённым гармоническим \\(\\sum\\frac{1}{n^p}\\)).',
        detail: `<p>Простая идея: если твой ряд "меньше" заведомо сходящегося — он тоже сходится. Если "больше" заведомо расходящегося — тоже расходится. Для сравнения берут эталонные ряды: геометрический или \\(\\sum\\frac{1}{n^p}\\).</p>
<p>Важно: члены должны быть неотрицательными, и неравенство должно выполняться для всех достаточно больших \\(n\\).</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\sum\\dfrac{1}{n^2+1}\\): так как \\(\\dfrac{1}{n^2+1}\\le\\dfrac{1}{n^2}\\) и \\(\\sum\\dfrac{1}{n^2}\\) сходится → наш ряд тоже сходится.</p>
<p>2)&nbsp; \\(\\sum\\dfrac{1}{\\sqrt{n}}\\): так как \\(\\dfrac{1}{\\sqrt{n}}\\ge\\dfrac{1}{n}\\) и \\(\\sum\\dfrac{1}{n}\\) расходится → наш ряд тоже расходится.</p>
<p>3)&nbsp; \\(\\sum\\dfrac{\\sin^2 n}{n^2}\\): так как \\(\\dfrac{\\sin^2 n}{n^2}\\le\\dfrac{1}{n^2}\\) и \\(\\sum\\dfrac{1}{n^2}\\) сходится → сходится.</p>`
      },
      {
        title: 'Эталонные ряды',
        formula: '\\[\\sum_{n=1}^{\\infty}\\frac{1}{n^p}: \\quad p > 1 \\Rightarrow \\text{сходится}, \\quad p \\le 1 \\Rightarrow \\text{расходится}\\]',
        note: 'Обобщённый гармонический ряд. Пример: \\(\\sum\\frac{1}{n^2}=\\frac{\\pi^2}{6}\\) — сходится; \\(\\sum\\frac{1}{n}\\) — расходится.',
        detail: `<p>Обобщённый гармонический ряд \\(\\sum\\frac{1}{n^p}\\) — главный "эталон" для сравнения. Запомни границу: \\(p>1\\) — сходится, \\(p\\le 1\\) — расходится.</p>
<p>При \\(p=1\\) получается знаменитый гармонический ряд \\(\\sum\\frac{1}{n}\\) — расходится (это нужно помнить). При \\(p=2\\) — сходится, и его сумма равна \\(\\frac{\\pi^2}{6}\\) (результат Эйлера, который поразил математический мир).</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\sum\\dfrac{1}{n^2}\\) (\\(p=2>1\\)): сходится, сумма \\(=\\dfrac{\\pi^2}{6}\\).</p>
<p>2)&nbsp; \\(\\sum\\dfrac{1}{n}\\) (\\(p=1\\)): расходится — классика!</p>
<p>3)&nbsp; \\(\\sum\\dfrac{1}{\\sqrt{n}}=\\sum\\dfrac{1}{n^{1/2}}\\) (\\(p=\\tfrac{1}{2}<1\\)): расходится.</p>`
      },
      {
        title: 'Степенные ряды',
        formula: '\\[\\sum_{n=0}^{\\infty} c_n(x-a)^n, \\quad R = \\frac{1}{\\limsup|c_n|^{1/n}}\\]',
        note: 'Радиус сходимости R определяет интервал (a−R, a+R), внутри которого ряд сходится абсолютно.',
        detail: `<p>Степенной ряд — это многочлен "бесконечной степени" вида \\(c_0+c_1(x-a)+c_2(x-a)^2+\\cdots\\). Такой ряд сходится не для всех \\(x\\), а только на некотором промежутке вокруг точки \\(a\\). Ширина этого промежутка — радиус сходимости \\(R\\).</p>
<p>Радиус сходимости удобно находить признаком Даламбера: \\(R=\\lim\\left|\\frac{c_n}{c_{n+1}}\\right|\\). На концах интервала нужно проверять отдельно.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\sum\\dfrac{x^n}{n}\\): Даламбер: \\(\\left|\\dfrac{x^{n+1}/(n+1)}{x^n/n}\\right|=\\dfrac{n}{n+1}|x|\\to|x|\\). Сходится при \\(|x|<1\\), то есть \\(R=1\\).</p>
<p>2)&nbsp; \\(e^x=\\sum\\dfrac{x^n}{n!}\\): \\(R=\\infty\\), сходится для всех \\(x\\).</p>
<p>3)&nbsp; \\(\\sum n!\\, x^n\\): Даламбер даёт \\((n+1)|x|\\to\\infty\\) при любом \\(x\\neq 0\\). \\(R=0\\), сходится только при \\(x=0\\).</p>`
      }
    ]
  },

  limits: {
    title: 'Пределы и непрерывность',
    accent: '#a855f7',
    icon: 'lim',
    backTo: 'limitsSection',
    steps: [
      {
        title: 'Определение предела',
        formula: '\\[\\lim_{x \\to a} f(x) = L\\]',
        note: 'f(x) приближается к L сколь угодно близко при x → a. Само значение f(a) может не существовать.',
        detail: `<p>Предел описывает, <strong>к чему стремится</strong> функция при приближении \\(x\\) к некоторой точке. Важно: само значение функции в этой точке не важно — функция может там даже не быть определена!</p>
<p>Если можно просто подставить \\(x=a\\) и ничего страшного не случится (нет деления на ноль и т.п.) — делаем это. Иначе — упрощаем выражение или применяем специальные методы.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\lim_{x\\to 2}x^2 = 4\\) — просто подставляем \\(x=2\\).</p>
<p>2)&nbsp; \\(\\lim_{x\\to 1}\\dfrac{x^2-1}{x-1} = \\lim_{x\\to 1}\\dfrac{(x-1)(x+1)}{x-1} = \\lim_{x\\to 1}(x+1) = 2\\) — дробь в точке не определена, но предел есть после сокращения.</p>
<p>3)&nbsp; \\(\\lim_{x\\to 0}\\dfrac{\\sin x}{x} = 1\\) — в точке \\(x=0\\) деление на ноль, но предел существует и равен 1.</p>`
      },
      {
        title: 'Первый замечательный предел',
        formula: '\\[\\lim_{x \\to 0} \\frac{\\sin x}{x} = 1\\]',
        note: 'Аналогично: \\(\\lim\\frac{\\tan x}{x}=1\\), \\(\\lim\\frac{\\arcsin x}{x}=1\\) при x→0. Пример: \\(\\lim\\frac{\\sin 3x}{x}=3\\)',
        detail: `<p>Это один из самых важных пределов. Суть: когда видишь \\(\\frac{\\sin(\\text{что-то})}{\\text{то же самое}}\\) и это "что-то" стремится к нулю — спокойно ставишь 1.</p>
<p>Ключевое: аргумент синуса и знаменатель должны быть <strong>одинаковыми</strong> выражениями. Если не так — приводим к этому виду, умножая и деля на нужное число.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\lim_{x\\to 0}\\dfrac{\\sin x}{x}=1\\) — классика.</p>
<p>2)&nbsp; \\(\\lim_{x\\to 0}\\dfrac{\\sin 3x}{x}\\): аргумент синуса \\(3x\\), а в знаменателе просто \\(x\\). Умножаем и делим на 3: \\(\\dfrac{\\sin 3x}{3x}\\cdot 3 \\to 1\\cdot 3 = \\mathbf{3}\\).</p>
<p>3)&nbsp; \\(\\lim_{x\\to 0}\\dfrac{\\sin 5x}{\\sin 3x}=\\dfrac{\\sin 5x}{5x}\\cdot\\dfrac{3x}{\\sin 3x}\\cdot\\dfrac{5}{3} \\to 1\\cdot 1\\cdot\\dfrac{5}{3} = \\mathbf{\\dfrac{5}{3}}\\).</p>`
      },
      {
        title: 'Второй замечательный предел',
        formula: '\\[\\lim_{x \\to \\infty}\\left(1 + \\frac{1}{x}\\right)^x = e \\qquad \\lim_{x\\to 0}(1+x)^{1/x} = e\\]',
        note: 'Число e ≈ 2.71828. Пример: \\(\\lim\\left(1+\\frac{2}{n}\\right)^n = e^2\\)',
        detail: `<p>Когда видишь выражение вида \\((1+\\text{что-то маленькое})^{1/\\text{то же самое}}\\) — это второй замечательный предел, результат равен \\(e\\). Если показатель в \\(k\\) раз больше, то \\(e^k\\).</p>
<p>Общая схема: \\(\\lim(1+\\alpha)^{1/\\alpha}=e\\) при \\(\\alpha\\to 0\\). Если в показателе стоит \\(k/\\alpha\\) — получаем \\(e^k\\).</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\lim_{n\\to\\infty}\\left(1+\\dfrac{1}{n}\\right)^n = e\\) — само определение числа \\(e\\).</p>
<p>2)&nbsp; \\(\\lim_{n\\to\\infty}\\left(1+\\dfrac{2}{n}\\right)^n\\): перепишем как \\(\\left[\\left(1+\\dfrac{2}{n}\\right)^{n/2}\\right]^2\\to e^2\\).</p>
<p>3)&nbsp; \\(\\lim_{x\\to 0}(1+x)^{1/x}=e\\) — альтернативная запись: \\(\\alpha=x\\to 0\\), показатель \\(=1/\\alpha\\).</p>`
      },
      {
        title: 'Правило Лопиталя',
        formula: "\\[\\lim_{x\\to a}\\frac{f(x)}{g(x)} = \\lim_{x\\to a}\\frac{f'(x)}{g'(x)} \\quad \\left[\\frac{0}{0}\\text{ или }\\frac{\\infty}{\\infty}\\right]\\]",
        note: 'Применяется при неопределённостях 0/0 или ∞/∞. Пример: \\(\\lim_{x\\to 0}\\frac{\\sin x}{x}=\\lim_{x\\to 0}\\frac{\\cos x}{1}=1\\)',
        detail: `<p>Если при подстановке получаешь \\(\\frac{0}{0}\\) или \\(\\frac{\\infty}{\\infty}\\) — это неопределённость. Правило Лопиталя: дифференцируй числитель и знаменатель <strong>по отдельности</strong> (не как дробь!), и снова бери предел.</p>
<p>Можно применять несколько раз подряд, если неопределённость сохраняется. Главное — сначала убедись, что у тебя именно \\(\\frac{0}{0}\\) или \\(\\frac{\\infty}{\\infty}\\).</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\lim_{x\\to 0}\\dfrac{\\sin x}{x}\\): тип \\(\\frac{0}{0}\\), дифференцируем → \\(\\dfrac{\\cos x}{1}\\to 1\\).</p>
<p>2)&nbsp; \\(\\lim_{x\\to\\infty}\\dfrac{x}{e^x}\\): тип \\(\\frac{\\infty}{\\infty}\\), дифференцируем → \\(\\dfrac{1}{e^x}\\to 0\\).</p>
<p>3)&nbsp; \\(\\lim_{x\\to 0}\\dfrac{e^x-1}{x}\\): тип \\(\\frac{0}{0}\\), дифференцируем → \\(\\dfrac{e^x}{1}\\to 1\\).</p>`
      },
      {
        title: 'Эквивалентные бесконечно малые',
        formula: '\\[x\\to 0:\\quad \\sin x \\sim x,\\quad \\tan x \\sim x,\\quad \\ln(1+x)\\sim x,\\quad e^x-1\\sim x\\]',
        note: 'При x→0 можно заменять в произведениях и частных. Пример: \\(\\lim\\frac{\\ln(1+x)}{\\sin x}=\\lim\\frac{x}{x}=1\\)',
        detail: `<p>При \\(x\\to 0\\) некоторые функции ведут себя "как \\(x\\)" — они имеют тот же порядок малости. Это значит, что в пределах (в произведениях и частных) их можно просто заменять на \\(x\\) — результат не изменится.</p>
<p>Это мощный приём для быстрого вычисления пределов без правила Лопиталя. Главное — заменять только в произведениях и частных, но <strong>не в суммах</strong>!</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\lim_{x\\to 0}\\dfrac{\\ln(1+x)}{\\sin x}\\): заменяем \\(\\ln(1+x)\\sim x\\) и \\(\\sin x\\sim x\\) → \\(\\dfrac{x}{x}=1\\).</p>
<p>2)&nbsp; \\(\\lim_{x\\to 0}\\dfrac{e^x-1}{x}\\): \\(e^x-1\\sim x\\) → \\(\\dfrac{x}{x}=1\\).</p>
<p>3)&nbsp; \\(\\lim_{x\\to 0}\\dfrac{\\tan 2x}{\\sin 3x}\\): \\(\\tan 2x\\sim 2x\\), \\(\\sin 3x\\sim 3x\\) → \\(\\dfrac{2x}{3x}=\\dfrac{2}{3}\\).</p>`
      },
      {
        title: 'Непрерывность функции',
        formula: '\\[f \\text{ непрерывна в } x_0 \\Leftrightarrow \\lim_{x\\to x_0}f(x) = f(x_0)\\]',
        note: 'Три условия: f(x₀) определена, предел существует, и предел равен значению функции.',
        detail: `<p>Непрерывность — это когда у функции нет "разрывов" и "прыжков". Формально нужно проверить три условия: функция определена в точке, предел в этой точке существует, и предел совпадает со значением функции.</p>
<p>Большинство "стандартных" функций непрерывны на своей области: полиномы, \\(e^x\\), \\(\\sin x\\), \\(\\cos x\\). Разрывы появляются в дробях (при нулевом знаменателе) и в кусочно-заданных функциях.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(f(x)=x^2\\) непрерывна везде: \\(\\lim_{x\\to 2}x^2=4=f(2)\\) ✓</p>
<p>2)&nbsp; \\(f(x)=\\dfrac{1}{x}\\) имеет разрыв при \\(x=0\\) — там функция не определена.</p>
<p>3)&nbsp; \\(f(x)=\\dfrac{\\sin x}{x}\\) при \\(x=0\\) не определена, но если <em>доопределить</em> \\(f(0)=1\\), функция станет непрерывной — потому что \\(\\lim_{x\\to 0}\\dfrac{\\sin x}{x}=1\\).</p>`
      },
      {
        title: 'Односторонние пределы',
        formula: '\\[\\lim_{x\\to a^-}f(x) = L^- \\qquad \\lim_{x\\to a^+}f(x) = L^+\\]',
        note: 'Предел существует ⟺ L⁻ = L⁺. Пример: у \\(|x|/x\\) при x→0: L⁻ = −1, L⁺ = 1 → предела нет.',
        detail: `<p>Иногда функция ведёт себя по-разному при подходе слева и справа. Тогда говорят об <strong>односторонних пределах</strong>: \\(x\\to a^-\\) — подходим слева (меньше \\(a\\)), \\(x\\to a^+\\) — справа (больше \\(a\\)).</p>
<p>Двусторонний предел существует только тогда, когда оба односторонних существуют <strong>и равны друг другу</strong>. Если они разные — предела нет.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(f(x)=\\dfrac{|x|}{x}\\): при \\(x\\to 0^-\\)→ \\(-1\\); при \\(x\\to 0^+\\)→ \\(+1\\). Разные → предела нет.</p>
<p>2)&nbsp; \\(f(x)=\\dfrac{1}{x-1}\\): при \\(x\\to 1^-\\)→ \\(-\\infty\\); при \\(x\\to 1^+\\)→ \\(+\\infty\\). Предела нет.</p>
<p>3)&nbsp; \\(f(x)=\\sqrt{x}\\) при \\(x\\to 0\\): левый предел не существует (корень из отрицательного), правый \\(= 0\\).</p>`
      },
      {
        title: 'Теорема о сжатой переменной',
        formula: '\\[g(x) \\le f(x) \\le h(x), \\quad \\lim g = \\lim h = L \\implies \\lim f = L\\]',
        note: 'Если функция зажата между двумя, стремящимися к одному пределу, то и она стремится к тому же. Пример: \\(\\lim x\\sin\\frac{1}{x}=0\\)',
        detail: `<p>Теорема о сжатой переменной (теорема "сэндвича") используется, когда напрямую вычислить предел трудно. Идея: если найти две функции, которые "зажимают" нашу с двух сторон и обе стремятся к одному числу — то и наша функция стремится к тому же.</p>
<p>Чаще всего применяется, когда функция — произведение ограниченной функции (\\(\\sin\\), \\(\\cos\\)) на что-то стремящееся к нулю.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(\\lim_{x\\to 0}x\\sin\\dfrac{1}{x}\\): синус ограничен, поэтому \\(-|x|\\le x\\sin\\dfrac{1}{x}\\le|x|\\), и \\(|x|\\to 0\\) → <strong>предел = 0</strong>.</p>
<p>2)&nbsp; \\(\\lim_{n\\to\\infty}\\dfrac{\\sin n}{n}\\): \\(-\\dfrac{1}{n}\\le\\dfrac{\\sin n}{n}\\le\\dfrac{1}{n}\\), и \\(\\dfrac{1}{n}\\to 0\\) → <strong>предел = 0</strong>.</p>
<p>3)&nbsp; \\(\\lim_{x\\to 0}x^2\\cos\\dfrac{1}{x}\\): \\(-x^2\\le x^2\\cos\\dfrac{1}{x}\\le x^2\\), и \\(x^2\\to 0\\) → <strong>предел = 0</strong>.</p>`
      }
    ]
  },

  ode: {
    title: 'Дифференциальные уравнения',
    accent: '#f97316',
    icon: "y'",
    backTo: 'odeSection',
    steps: [
      {
        title: 'Прямое интегрирование',
        formula: "\\[y' = f(x) \\implies y = \\int f(x)\\,dx + C\\]",
        note: `Простейший случай: правая часть зависит только от x. Пример: \\(y'=3x^2 \\implies y=x^3+C\\)`,
        detail: `<p>Самый простой тип ДУ: производная функции равна некоторому выражению, зависящему только от \\(x\\). Решение — просто проинтегрировать обе части. Не забудь константу \\(C\\) — она отражает то, что решений бесконечно много (целое семейство кривых).</p>
<p>Конкретное значение \\(C\\) находится, если дополнительно задано начальное условие.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(y'=3x^2\\) → \\(y=\\int 3x^2\\,dx = x^3+C\\).</p>
<p>2)&nbsp; \\(y'=\\cos x\\) → \\(y=\\int\\cos x\\,dx = \\sin x+C\\).</p>
<p>3)&nbsp; \\(y'=e^x+1\\) → \\(y=\\int(e^x+1)\\,dx = e^x+x+C\\).</p>`
      },
      {
        title: 'Экспоненциальный рост и убывание',
        formula: "\\[y' = ky \\implies y = Ce^{kx}\\]",
        note: `k > 0 — рост, k < 0 — убывание. Пример: задача Коши \\(y'=2y,\\,y(0)=3 \\implies y=3e^{2x}\\)`,
        detail: `<p>Это уравнение встречается везде: рост бактерий, радиоактивный распад, начисление процентов. Смысл: <em>скорость изменения пропорциональна текущему значению</em>. Решение всегда экспоненциально.</p>
<p>При \\(k>0\\) функция растёт, при \\(k<0\\) — убывает. Константа \\(C\\) определяется начальным условием.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(y'=2y,\\ y(0)=3\\): общее \\(y=Ce^{2x}\\); из \\(y(0)=C=3\\) → \\(y=3e^{2x}\\).</p>
<p>2)&nbsp; \\(y'=-y\\) (затухание): \\(y=Ce^{-x}\\), амплитуда убывает.</p>
<p>3)&nbsp; \\(y'=0{,}05y,\\ y(0)=1000\\): \\(y=1000e^{0{,}05x}\\) — модель роста капитала.</p>`
      },
      {
        title: 'Разделение переменных',
        formula: '\\[\\frac{dy}{dx} = f(x)\\cdot g(y) \\implies \\int\\frac{dy}{g(y)} = \\int f(x)\\,dx + C\\]',
        note: 'Переносим всё с y влево, всё с x — вправо, затем интегрируем обе части независимо.',
        detail: `<p>Если правую часть ДУ можно записать как произведение функции от \\(x\\) и функции от \\(y\\) — переносим всё с \\(y\\) влево, с \\(x\\) — вправо, и интегрируем. Это самый общий метод для нелинейных ДУ первого порядка.</p>
<p>Формально "разделяем" \\(dy\\) и \\(dx\\) как отдельные символы. После интегрирования выражаем \\(y\\) явно, если возможно.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(y'=xy\\): \\(\\dfrac{dy}{y}=x\\,dx\\) → \\(\\ln|y|=\\dfrac{x^2}{2}+C\\) → \\(y=Ae^{x^2/2}\\).</p>
<p>2)&nbsp; \\(y'=y^2\\): \\(\\dfrac{dy}{y^2}=dx\\) → \\(-\\dfrac{1}{y}=x+C\\) → \\(y=-\\dfrac{1}{x+C}\\).</p>
<p>3)&nbsp; \\(y'=\\dfrac{x}{y}\\): \\(y\\,dy=x\\,dx\\) → \\(\\dfrac{y^2}{2}=\\dfrac{x^2}{2}+C\\) → \\(y^2-x^2=C\\) (гиперболы).</p>`
      },
      {
        title: 'Линейное ДУ первого порядка',
        formula: "\\[y' + P(x)\\,y = Q(x), \\quad \\mu = e^{\\int P(x)\\,dx}\\]",
        note: 'Решается умножением на интегрирующий множитель μ. Тогда (μy)′ = μQ(x), и берём интеграл.',
        detail: `<p>Переменные здесь не разделяются напрямую. Метод: находим <strong>интегрирующий множитель</strong> \\(\\mu=e^{\\int P(x)\\,dx}\\) и умножаем на него обе части. Левая часть превращается в производную произведения \\((\\mu y)'\\), после чего всё интегрируется.</p>
<p>Шаги: 1) найди \\(\\mu\\); 2) умножь всё уравнение на \\(\\mu\\); 3) заметь, что слева \\((\\mu y)'\\); 4) интегрируй обе части.</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(y'+y=e^x\\): \\(\\mu=e^x\\); умножаем: \\((e^x y)'=e^{2x}\\) → \\(e^x y=\\dfrac{e^{2x}}{2}+C\\) → \\(y=\\dfrac{e^x}{2}+Ce^{-x}\\).</p>
<p>2)&nbsp; \\(y'-2y=1\\): \\(P=-2\\), \\(\\mu=e^{-2x}\\); \\((e^{-2x}y)'=e^{-2x}\\) → \\(y=-\\dfrac{1}{2}+Ce^{2x}\\).</p>`
      },
      {
        title: 'Задача Коши',
        formula: "\\[y' = f(x,\\,y), \\quad y(x_0) = y_0\\]",
        note: `Начальное условие выделяет единственное решение. Пример: \\(y'=y,\\,y(0)=1 \\implies y=e^x\\)`,
        detail: `<p>Общее решение ДУ содержит произвольную константу \\(C\\) — это целое семейство кривых. Задача Коши добавляет одно условие: при \\(x=x_0\\) функция принимает значение \\(y_0\\). Это "фиксирует" одну конкретную кривую из семейства.</p>
<p>Алгоритм: сначала найди общее решение (с \\(C\\)), затем подставь начальное условие и вычисли \\(C\\).</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(y'=2x,\\ y(0)=1\\): общее \\(y=x^2+C\\); условие: \\(0+C=1\\) → \\(C=1\\) → \\(y=x^2+1\\).</p>
<p>2)&nbsp; \\(y'=y,\\ y(0)=5\\): общее \\(y=Ce^x\\); условие: \\(C=5\\) → \\(y=5e^x\\).</p>
<p>3)&nbsp; \\(y'=\\cos x,\\ y(0)=0\\): общее \\(y=\\sin x+C\\); условие: \\(0+C=0\\) → \\(y=\\sin x\\).</p>`
      },
      {
        title: 'Однородные уравнения',
        formula: "\\[y' = f\\!\\left(\\frac{y}{x}\\right) \\xrightarrow{t=y/x} \\text{разделение переменных}\\]",
        note: 'Замена t = y/x превращает однородное ДУ в уравнение с разделяемыми переменными.',
        detail: `<p>Однородное ДУ — когда правая часть зависит только от отношения \\(y/x\\). Напрямую переменные не разделяются. Но замена \\(t=y/x\\) (то есть \\(y=tx\\)) превращает его в уравнение с разделяющимися переменными для \\(t\\).</p>
<p>После замены: \\(y'=t+xt'\\). Подставляем, разделяем переменные, интегрируем, возвращаем к \\(y\\) через \\(y=tx\\).</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(y'=\\dfrac{y}{x}\\): после замены \\(t+xt'=t\\) → \\(xt'=0\\) → \\(t=C\\) → \\(y=Cx\\).</p>
<p>2)&nbsp; \\(y'=\\dfrac{y}{x}+1\\): \\(t+xt'=t+1\\) → \\(xt'=1\\) → \\(dt=\\dfrac{dx}{x}\\) → \\(t=\\ln|x|+C\\) → \\(y=x(\\ln|x|+C)\\).</p>`
      },
      {
        title: 'ДУ второго порядка с пост. коэфф.',
        formula: "\\[y'' + py' + qy = 0, \\quad k^2 + pk + q = 0\\]",
        note: 'Составляем характеристическое уравнение. Корни k₁, k₂ определяют вид решения.',
        detail: `<p>Для однородного ДУ второго порядка с постоянными коэффициентами ищем решение в виде \\(y=e^{kx}\\). Подставляем в уравнение, выносим \\(e^{kx}\\) (он никогда не ноль) и получаем <strong>характеристическое уравнение</strong> — обычное квадратное.</p>
<p>Находим корни этого квадратного уравнения и по их типу записываем общее решение (см. следующий шаг).</p>
<p><strong>Примеры:</strong></p>
<p>1)&nbsp; \\(y''-3y'+2y=0\\): \\(k^2-3k+2=0\\) → \\((k-1)(k-2)=0\\) → \\(k_1=1,\\ k_2=2\\).</p>
<p>2)&nbsp; \\(y''+4y'+4y=0\\): \\(k^2+4k+4=0\\) → \\((k+2)^2=0\\) → \\(k=-2\\) (кратный корень).</p>
<p>3)&nbsp; \\(y''+y=0\\): \\(k^2+1=0\\) → \\(k=\\pm i\\) (чисто мнимые корни).</p>`
      },
      {
        title: 'Типы решений по корням',
        formula: "\\[k_1 \\neq k_2 \\in \\mathbb{R}:\\; C_1e^{k_1 x}+C_2e^{k_2 x} \\quad k_{1,2}=\\alpha\\pm\\beta i:\\; e^{\\alpha x}(C_1\\cos\\beta x+C_2\\sin\\beta x)\\]",
        note: 'Три случая: различные вещественные корни, кратный корень, комплексные сопряжённые корни.',
        detail: `<p>Три возможных случая для корней характеристического уравнения — три разных вида общего решения. Это нужно просто запомнить как таблицу.</p>
<p>Случай с комплексными корнями \\(\\alpha\\pm\\beta i\\) даёт колебательный характер решения: \\(e^{\\alpha x}\\) отвечает за рост или затухание, \\(\\cos\\) и \\(\\sin\\) — за сами колебания.</p>
<p><strong>Примеры (по каждому случаю):</strong></p>
<p>1)&nbsp; \\(k_1=1,\\ k_2=2\\) (различные вещественные): \\(y=C_1e^x+C_2e^{2x}\\).</p>
<p>2)&nbsp; \\(k_1=k_2=-2\\) (кратный корень): \\(y=(C_1+C_2 x)e^{-2x}\\) — к экспоненте домножается \\(x\\).</p>
<p>3)&nbsp; \\(k=\\pm 3i\\) (\\(\\alpha=0,\\ \\beta=3\\)): \\(y=C_1\\cos 3x+C_2\\sin 3x\\) — чистые колебания без затухания.</p>`
      }
    ]
  },

  probability: {
    title: 'Теория вероятностей',
    accent: '#38bdf8',
    icon: 'P',
    backTo: 'probabilitySection',
    steps: [
      {
        title: 'Случайные события и пространство элементарных исходов',
        formula: '\\[\\Omega = \\{\\omega_1, \\omega_2, \\ldots, \\omega_n\\}, \\quad A \\subseteq \\Omega\\]',
        note: 'Эксперимент → множество всех исходов Ω (пространство). Событие A — любое подмножество Ω. Достоверное: A=Ω; Невозможное: A=∅.',
        detail: `<p><strong>Случайный эксперимент</strong> — опыт, результат которого нельзя предсказать заранее. Пример: бросок кубика.</p>
<p><strong>Элементарный исход</strong> ω — неделимый результат эксперимента. Для кубика: ω ∈ {1,2,3,4,5,6}.</p>
<p><strong>Событие A</strong> — набор исходов. «Выпало чётное» = {2,4,6} ⊆ Ω.</p>
<p><strong>Типы:</strong> Достоверное (A=Ω, всегда происходит), невозможное (A=∅, никогда), противоположное (Ā = Ω∖A).</p>`
      },
      {
        title: 'Классическая вероятность',
        formula: '\\[P(A) = \\frac{m}{n}\\]',
        note: 'n — число равновозможных исходов, m — число исходов, благоприятных для A. Всегда 0 ≤ P(A) ≤ 1.',
        detail: `<p>Модель применима, когда все элементарные исходы <strong>равновозможны</strong>.</p>
<p><strong>Пример 1.</strong> Кубик: P(чётное) = 3/6 = 0,5.</p>
<p><strong>Пример 2.</strong> Урна с 5 красными и 3 белыми шарами. Вытаскиваем 1: P(красный) = 5/8.</p>
<p><strong>Пример 3.</strong> Два кубика: P(сумма=7) = 6/36 = 1/6 (пары: (1,6),(2,5),(3,4),(4,3),(5,2),(6,1)).</p>`
      },
      {
        title: 'Комбинаторика: сочетания, перестановки, размещения',
        formula: '\\[C_n^k = \\binom{n}{k} = \\frac{n!}{k!(n-k)!}, \\quad A_n^k = \\frac{n!}{(n-k)!}, \\quad P_n = n!\\]',
        note: 'C — выбор без учёта порядка; A — с учётом порядка; P — все перестановки n элементов.',
        detail: `<p><strong>Сочетания</strong> C(n,k): сколькими способами выбрать k из n, порядок не важен. C(5,2)=10.</p>
<p><strong>Размещения</strong> A(n,k): выбрать k из n с учётом порядка. A(5,2)=20.</p>
<p><strong>Перестановки</strong> P(n)=n!: все способы расставить n объектов. P(4)=24.</p>
<p><strong>Пример.</strong> Лотерея: из 36 чисел выбираем 6. Число комбинаций: C(36,6) = 1 947 792.</p>`
      },
      {
        title: 'Алгебра событий: сумма, произведение, дополнение',
        formula: '\\[P(A \\cup B) = P(A) + P(B) - P(A \\cap B)\\]',
        note: 'Для несовместных событий (A∩B=∅): P(A∪B)=P(A)+P(B). Дополнение: P(Ā)=1−P(A).',
        detail: `<p><strong>Сумма A∪B</strong> — произошло хотя бы одно из A, B.</p>
<p><strong>Произведение A∩B</strong> — произошли оба события.</p>
<p><strong>Формула включений-исключений</strong> убирает двойной счёт пересечения.</p>
<p><strong>Пример.</strong> Карта из колоды: P(туз) = 4/52, P(червы) = 13/52, P(туз червей) = 1/52. P(туз или червы) = 4/52+13/52−1/52 = 16/52 ≈ 0,308.</p>`
      },
      {
        title: 'Условная вероятность',
        formula: '\\[P(A|B) = \\frac{P(A \\cap B)}{P(B)}, \\quad P(B) > 0\\]',
        note: 'Вероятность A при условии, что B уже произошло. Пространство «сужается» до B.',
        detail: `<p>Когда появляется новая информация (событие B произошло), исходное пространство Ω сужается до B.</p>
<p><strong>Пример 1.</strong> Кубик: P(выпало >4 | выпало чётное) = P({6})/P({2,4,6}) = (1/6)/(3/6) = 1/3.</p>
<p><strong>Пример 2.</strong> Из 10 деталей 3 бракованные. Берём 2 без возврата. P(2-я брак | 1-я брак) = 2/9.</p>`
      },
      {
        title: 'Независимые события. Умножение вероятностей',
        formula: '\\[P(A \\cap B) = P(A) \\cdot P(B) \\quad \\text{(если A и B независимы)}\\]',
        note: 'События независимы, если знание об одном не меняет вероятность другого: P(A|B)=P(A).',
        detail: `<p>Независимость — свойство, а не очевидный факт. Всегда проверяйте, не влияет ли первое событие на условия второго.</p>
<p><strong>Пример 1.</strong> Два броска монеты. P(орёл и орёл) = 0,5 × 0,5 = 0,25.</p>
<p><strong>Пример 2.</strong> Три независимых устройства с надёжностью 0,9 каждое. P(все работают) = 0,9³ = 0,729.</p>
<p><strong>Пример 3.</strong> P(хотя бы одно откажет) = 1 − 0,729 = 0,271.</p>`
      },
      {
        title: 'Формула полной вероятности',
        formula: '\\[P(A) = \\sum_{i=1}^{n} P(H_i) \\cdot P(A|H_i)\\]',
        note: 'H₁,…,Hₙ — полная группа гипотез (несовместные, ∑P(Hᵢ)=1). Применяется, когда A зависит от неизвестной причины.',
        detail: `<p>Когда напрямую вычислить P(A) сложно, делим пространство на несовместные гипотезы (причины).</p>
<p><strong>Пример.</strong> Завод состоит из 3 цехов: цех 1 выпускает 50% продукции с 2% брака, цех 2 — 30% с 3% брака, цех 3 — 20% с 5% брака.</p>
<p>P(брак) = 0,5×0,02 + 0,3×0,03 + 0,2×0,05 = 0,01+0,009+0,01 = 0,029 = 2,9%.</p>`
      },
      {
        title: 'Формула Байеса',
        formula: '\\[P(H_i|A) = \\frac{P(H_i) \\cdot P(A|H_i)}{\\displaystyle\\sum_{j=1}^{n} P(H_j) \\cdot P(A|H_j)}\\]',
        note: 'Обновляет вероятность гипотезы Hᵢ после наблюдения события A (апостериорная вероятность).',
        detail: `<p>Байес позволяет «перевернуть» условную вероятность: знаем P(A|Hᵢ), находим P(Hᵢ|A).</p>
<p><strong>Пример.</strong> Из задачи про заводы выше: выбрана случайная деталь и оказалась бракованной. Из какого цеха?</p>
<p>P(H₁|брак) = (0,5×0,02)/0,029 ≈ 0,345 (34,5%).</p>
<p>P(H₂|брак) = (0,3×0,03)/0,029 ≈ 0,310 (31,0%).</p>
<p>P(H₃|брак) = (0,2×0,05)/0,029 ≈ 0,345 (34,5%).</p>`
      },
      {
        title: 'Формула Бернулли',
        formula: '\\[P_n(k) = C_n^k \\, p^k q^{n-k}, \\quad q = 1 - p\\]',
        note: 'Вероятность ровно k успехов в n независимых испытаниях с вероятностью успеха p.',
        detail: `<p>Условия: испытания независимы, вероятность успеха p одинакова во всех испытаниях.</p>
<p><strong>Пример 1.</strong> Монета брошена 5 раз. P(ровно 3 орла) = C(5,3)×0,5³×0,5² = 10×0,125×0,25 = 0,3125.</p>
<p><strong>Пример 2.</strong> Стрелок попадает с p=0,8. Стреляет 4 раза. P(ровно 3 попадания) = C(4,3)×0,8³×0,2 = 4×0,512×0,2 ≈ 0,410.</p>`
      },
      {
        title: 'Дискретная случайная величина. Математическое ожидание',
        formula: '\\[M(X) = \\sum_{i} x_i \\cdot p_i, \\quad D(X) = M(X^2) - [M(X)]^2\\]',
        note: 'M(X) — взвешенное среднее. D(X) — дисперсия (разброс). σ = √D(X) — стандартное отклонение.',
        detail: `<p><strong>Закон распределения</strong> — таблица {xᵢ, pᵢ}. Сумма всех pᵢ = 1.</p>
<p><strong>Пример.</strong> Кубик: X — число очков. M(X) = (1+2+3+4+5+6)/6 = 3,5. D(X) = M(X²)−[M(X)]² = (1+4+9+16+25+36)/6 − 3,5² = 91/6 − 12,25 ≈ 2,917.</p>
<p><strong>Свойства:</strong> M(aX+b) = aM(X)+b; D(aX) = a²D(X); D(X+Y) = D(X)+D(Y) при независимости.</p>`
      },
      {
        title: 'Биномиальное и пуассоновское распределения',
        formula: '\\[\\text{Bin}(n,p):\\; M=np,\\; D=npq \\qquad \\text{Poisson}(\\lambda):\\; P(X{=}k)=\\frac{\\lambda^k}{k!}e^{-\\lambda},\\; M=D=\\lambda\\]',
        note: 'Пуассон — предел биномиального при n→∞, p→0, np=λ. Применяется для редких событий (аварии, звонки, ошибки).',
        detail: `<p><strong>Биномиальное B(n,p):</strong> число успехов из n испытаний. Пример: 100 бросков монеты — количество орлов. M=50, D=25.</p>
<p><strong>Пуассоновское Pois(λ):</strong> число событий за период при постоянном потоке. Пример: λ=3 звонка/мин.</p>
<p>P(X=0)=e⁻³≈0,050; P(X=1)=3e⁻³≈0,149; P(X=2)=4,5e⁻³≈0,224.</p>`
      },
      {
        title: 'Геометрическое распределение',
        formula: '\\[P(X = k) = q^{k-1}p, \\quad M(X) = \\frac{1}{p}, \\quad D(X) = \\frac{q}{p^2}\\]',
        note: 'X — номер первого успеха в серии бернуллиевских испытаний. k = 1, 2, 3, …',
        detail: `<p>Применяется: «сколько попыток до первой удачи?»</p>
<p><strong>Пример.</strong> Вероятность попасть в цель — 0,3. Сколько выстрелов в среднем до первого попадания? M(X) = 1/0,3 ≈ 3,33 выстрела.</p>
<p>P(X=1)=0,3; P(X=2)=0,7×0,3=0,21; P(X=3)=0,49×0,3=0,147; …</p>`
      },
      {
        title: 'Нормальное распределение',
        formula: '\\[f(x) = \\frac{1}{\\sigma\\sqrt{2\\pi}}\\,e^{-\\frac{(x-\\mu)^2}{2\\sigma^2}}, \\quad P(a<X<b) = \\Phi\\!\\left(\\frac{b-\\mu}{\\sigma}\\right) - \\Phi\\!\\left(\\frac{a-\\mu}{\\sigma}\\right)\\]',
        note: 'Φ(x) — функция Лапласа (таблица). Правило трёх сигм: P(|X−μ|<3σ) ≈ 0,997.',
        detail: `<p>Нормальное N(μ,σ²) — самое важное непрерывное распределение (ЦПТ, физические измерения, ошибки).</p>
<p><strong>Стандартизация:</strong> Z = (X−μ)/σ ~ N(0,1).</p>
<p><strong>Пример.</strong> Рост студентов: μ=175 см, σ=8 см. P(170<X<185) = Φ((185−175)/8) − Φ((170−175)/8) = Φ(1,25) − Φ(−0,625) ≈ 0,894−0,266 = 0,628.</p>
<p><strong>Правило σ:</strong> P(|X−μ|<σ)≈0,683; P(|X−μ|<2σ)≈0,954; P(|X−μ|<3σ)≈0,997.</p>`
      },
      {
        title: 'Теорема Муавра–Лапласа (нормальное приближение Бернулли)',
        formula: '\\[P_n(k) \\approx \\frac{1}{\\sqrt{npq}}\\,\\varphi\\!\\left(\\frac{k - np}{\\sqrt{npq}}\\right), \\quad \\varphi(x)=\\frac{1}{\\sqrt{2\\pi}}e^{-x^2/2}\\]',
        note: 'Применяется при n≥30, p и q не близки к 0. Замена суммы биномиальных вероятностей интегралом нормального закона.',
        detail: `<p><strong>Локальная форма:</strong> вероятность ровно k успехов из n при больших n.</p>
<p><strong>Интегральная форма:</strong> P(k₁≤X≤k₂) = Φ(x₂)−Φ(x₁), где xᵢ = (kᵢ−np)/√(npq).</p>
<p><strong>Пример.</strong> Монета, n=400 бросков. P(180≤X≤220) = Φ(2)−Φ(−2) ≈ 2×0,4772 = 0,9544.</p>`
      },
      {
        title: 'Неравенство Чебышёва и закон больших чисел',
        formula: '\\[P\\!\\left(|X - M(X)| \\geq \\varepsilon\\right) \\leq \\frac{D(X)}{\\varepsilon^2}\\]',
        note: 'Чебышёв оценивает вероятность отклонения от среднего. ЗБЧ: выборочное среднее сходится к M(X) при n→∞.',
        detail: `<p><strong>Неравенство Чебышёва</strong> не требует знания распределения — только M(X) и D(X).</p>
<p><strong>Пример.</strong> M(X)=50, D(X)=100. P(|X−50|≥20) ≤ 100/400 = 0,25.</p>
<p><strong>ЗБЧ (Хинчин):</strong> если X₁,…,Xₙ — н.о.р. с M(Xᵢ)=μ, то выборочное среднее X̄ₙ→μ при n→∞ по вероятности.</p>
<p>Это теоретическое обоснование того, почему «среднее по большой выборке близко к истинному среднему».</p>`
      },
      {
        title: 'Центральная предельная теорема (ЦПТ)',
        formula: '\\[\\frac{\\bar{X}_n - \\mu}{\\sigma/\\sqrt{n}} \\xrightarrow{d} N(0,1) \\quad \\text{при } n \\to \\infty\\]',
        note: 'Сумма n н.о.р. случайных величин стремится к нормальному распределению при n→∞ (независимо от исходного закона).',
        detail: `<p>ЦПТ объясняет, почему нормальное распределение встречается везде: любое суммарное явление из многих независимых факторов — нормально.</p>
<p><strong>Пример.</strong> Страховая компания: каждый клиент требует выплату X с M=1000, σ=500. Для n=1000 клиентов общая выплата S ~ N(1 000 000, 500²×1000). P(S > 1 050 000) = P(Z > (50 000)/(500√1000)) = P(Z > 3,16) ≈ 0,0008.</p>`
      },
      {
        title: 'Цепи Маркова и ковариация',
        formula: '\\[\\text{Cov}(X,Y)=M(XY)-M(X)M(Y), \\quad r_{XY}=\\frac{\\text{Cov}(X,Y)}{\\sigma_X\\sigma_Y}\\]',
        note: 'Ковариация — линейная зависимость между X и Y. r∈[−1,1]: r=0 — нет линейной связи; |r|=1 — полная линейная связь.',
        detail: `<p><strong>Коэффициент корреляции r</strong> показывает силу и направление линейной зависимости.</p>
<p><strong>Цепи Маркова:</strong> случайный процесс, где будущее зависит только от текущего состояния (не от прошлого). Матрица переходных вероятностей P: P[i][j] = P(перейти из состояния i в j).</p>
<p><strong>Пример.</strong> Погода: P(ясно|ясно)=0,7, P(дождь|ясно)=0,3, P(ясно|дождь)=0,4, P(дождь|дождь)=0,6. Стационарное: π(ясно)=4/7≈0,571.</p>`
      }
    ]
  }
}

let _theoryKey = null

window.showTheory = function (key) {
  // Перенаправляем на полный браузер глав, если он доступен
  const CHAPTER_BROWSER_KEYS = ['integrals', 'derivatives', 'series', 'limits', 'ode']
  if (CHAPTER_BROWSER_KEYS.includes(key) && window.showSectionTheory) {
    window.showSectionTheory(key)
    return
  }

  _theoryKey = key
  const d = THEORY_DATA[key]
  if (!d) return

  const hex2rgba = (hex, a) => {
    const r = parseInt(hex.slice(1, 3), 16)
    const g = parseInt(hex.slice(3, 5), 16)
    const b = parseInt(hex.slice(5, 7), 16)
    return `rgba(${r},${g},${b},${a})`
  }

  const stepsHtml = d.steps.map((s, i) => `
    <div class="theory-step">
      <div class="theory-step-num"
           style="background:${hex2rgba(d.accent, 0.12)};color:${d.accent};border:2px solid ${hex2rgba(d.accent, 0.3)}">
        ${i + 1}
      </div>
      <div class="theory-step-body">
        <div class="theory-step-title">${s.title}</div>
        <div class="theory-step-formula">${s.formula}</div>
        <div class="theory-step-note">${s.note}</div>
        ${s.detail ? `<div class="theory-step-detail">${s.detail}</div>` : ''}
      </div>
    </div>
  `).join('')

  document.getElementById('theoryPageContent').innerHTML = `
    <div class="theory-header" style="border-bottom:2px solid ${hex2rgba(d.accent, 0.25)}">
      <div class="theory-header-icon" style="color:${d.accent}">${d.icon}</div>
      <h2 class="theory-header-title">${d.title}</h2>
      <p class="theory-header-sub">${d.steps.length} шагов · Базовые формулы и правила</p>
    </div>
    <div style="margin-top:1.5rem">${stepsHtml}</div>
    <button onclick="backFromTheory()"
            class="theory-start-btn"
            style="background:${d.accent}">
      Перейти к тесту →
    </button>
  `

  showPage('theoryPage')
  window.scrollTo(0, 0)
  if (window.MathJax) MathJax.typesetPromise()
}

window.backFromTheory = function () {
  if (_theoryKey && THEORY_DATA[_theoryKey]) {
    showPage(THEORY_DATA[_theoryKey].backTo)
    window.scrollTo(0, 0)
  }
}
